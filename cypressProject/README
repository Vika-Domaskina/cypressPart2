Install cypress:
npm install cypress --save -dev

Install all dependencies from package.json:
npm i

Run cypress:
npm run cypress:open

or you can run cypress in command line with reporter mochawesome:
./node_modules/.bin/cypress run